"""ZitiBot vision helpers (Gemini pointing, RealSense RGB-D, grasp demos).

Application entry points live next door under ``python_control/``:

- ``vision_controller.py`` — live Gemini + RealSense + **OpenSai Redis** cartesian goals
- ``gemini_image_cli.py`` — one-shot Gemini on a saved image (no Redis, no camera)
"""
